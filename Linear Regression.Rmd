---
title: "Linear Regression"
author: "Emmanuel NTAWUYIRUSHA(Student ID:101287)"
date: "2026-01-25"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
# Statistical Learning
## 2.3.1 Basic Commands
### functions to perform operations
#### using <- 

x <- c(1, 3, 2, 5)
x


#### using =
x = c(1, 6, 2)
x

y = c(1, 4, 3)
y

#### check length
#### length X
length(x)
#### length Y
length(y)

#### x+y
x+y

#### ls() function allows us to look at a list of all of the objects
ls()
#### rm() function can be ls() used to delete any that we don’t want
# rm(covax_new_data)


#### remove all objects at once
#rm(list = ls())  #remove all objects at once

#### ls() function allows us to look at a list of all of the objects
ls()

# Matrix
?matrix

#### create a simple matrix.
x <- matrix(data = c(1, 2, 3, 4), nrow = 2, ncol = 2)
x

#### we could just as well omit typing data=, nrow=, and ncol= in the matrix() command above
x <- matrix(c(1, 2, 3, 4), 2, 2)

#### Alternatively, the byrow = TRUE option can be used to populate the matrix in order of the rows.
matrix(c(1, 2, 3, 4), 2, 2, byrow = TRUE)


#### The sqrt() function returns the square root of each element
sqrt(x)

#### The command x2̂ raises each element of x sqrt() to the power 2;
x^2

#### The rnorm() function generates a vector of random normal variables, with first argument n the sample size.
x <- rnorm(50)
y <- x + rnorm(50, mean = 50, sd = .1)
cor(x, y)

#### The rnorm() function generates a vector of random normal variables,
x <- rnorm(50)
y <- x + rnorm(50, mean = 50, sd = .1)
cor(x, y)

#### By default, rnorm() creates standard normal random variables with a mean of 0 and a standard deviation of 1.
set.seed (1303)
rnorm(50)

#### We use set.seed() throughout the labs whenever we perform calculations involving random quantities.
set.seed(3)
y <- rnorm(100)
mean(y)
#[1] 0.0110
var(y)
#[1] 0.7329
sqrt(var(y))
#[1] 0.8561
sd(y)
#[1] 0.8561

```
```{r}
# 2.3.2 Graphics
#### The plot() function is the primary way to plot data in R.
x <- rnorm(100)
y <- rnorm(100)
plot(x, y)
plot(x, y, xlab = "this is the x-axis",
     ylab = "this is the y-axis",
     main = "Plot of X vs Y")

# page 46
```





```{r}
# 3 Linear Regression

## 3.6 Lab: Linear Regression
### 3.6.1 Libraries
library(MASS)
#install.packages("ISLR2")
library(ISLR2)

### 3.6.2 Simple Linear Regression
#### The ISLR2 library contains the Boston data set, which records medv (median house value) for 506 census tracts in Boston.
head(Boston)

#### To find out more about the data set, we can type ?Boston
?Boston

#### The basic lm() syntax is lm(y ∼ x, data)
#lm.fit <- lm(medv ~ lstat)
## The command causes an error because R does not know where to find the variables medv and lstat

#### If we attach Boston, the first line works fine because R now recognizes the variables.
lm.fit <- lm(medv ~ lstat , data = Boston)
attach(Boston)
lm.fit <- lm(medv ~ lstat)

#### If we type lm.fit, some basic information about the model is output.
lm.fit

#### For more detailed information, we use summary(lm.fit).
summary(lm.fit)

#### We can use the names() function in order to find out what other pieces of information are stored in lm.fit
names(lm.fit)

#### functions like coef() to access them.
coef(lm.fit)

#### coef(lm.fit)
coef(lm.fit)

#### In order to obtain a confidence interval for the coefficient estimates
confint(lm.fit)

#### The predict() function can be used to produce confidence intervals and prediction intervals prediction intervals for the prediction of medv for a given value of lstat
predict(lm.fit , data.frame(lstat = (c(5, 10, 15))),
        interval = "confidence")

predict(lm.fit , data.frame(lstat = (c(5, 10, 15))),
        interval = "prediction")

#### plot medv and lstat
plot(lstat , medv)
abline(lm.fit)

#### The abline() function can be used to draw any line
abline(lm.fit , lwd = 3)
abline(lm.fit , lwd = 3, col = "red")
plot(lstat , medv , col = "red")
plot(lstat , medv , pch = 20)
plot(lstat , medv , pch = "+")
plot(1:20, 1:20, pch = 1:20)

#### Next we examine some diagnostic plots
par(mfrow = c(2, 2))
plot(lm.fit)

#PP114

```
```




